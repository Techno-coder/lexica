fn factorial(n: u64) -> u64 {
	let ~result = 1;
	let ~counter = 1;
	loop counter == 1 => counter == n + 1 {
		result *= counter;
		counter += 1;
	}

	// Implicit drop of `n`
	drop counter = n + 1;
	result
}

fn main() -> u64 {
	factorial(7)
}
